agents:
  programming:
    type: "aider"
    max_retries: 8
    # model: "gpt-4o-mini"
    model: "o1-mini"
    model_settings:
      edit_format: "diff"
      verify_ssl: true
      timeout: 120
      max_chat_history_tokens: 100000
  testing:
    type: "aider"
    model: "gpt-4o-mini"

logging:
  tdac:
    level: DEBUG
    color: green
  other_packages:
    level: "INFO"